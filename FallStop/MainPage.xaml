<?xml version="1.0" encoding="utf-8" ?>
<local:BaseShake xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                 xmlns:local="clr-namespace:FallStop"
                 x:Class="FallStop.MainPage"
                 Title="FallStop"
                 BackgroundColor="{AppThemeBinding Light=#F0F5FF, Dark=#1F2A4A}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <VerticalStackLayout Grid.Row="1" Padding="20" Spacing="15" VerticalOptions="Center">

            <Border HeightRequest="250"
                    WidthRequest="250"
                    HorizontalOptions="Center"
                    StrokeThickness="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="50"/>
                </Border.StrokeShape>
                <Image Source="fall_stop.png" Aspect="AspectFill"/>
            </Border>

            <Label Padding="10"/>

            <Label x:Name="SensorStatusLabel"
                   Text="El sensor de caídas está monitoreando."
                   FontSize="16"
                   HorizontalOptions="Center"
                   HorizontalTextAlignment="Center"
                   TextColor="{AppThemeBinding Light=#3A2C6A, Dark=#A0A8D0}"/>

            <Button x:Name="ToggleSensorButton"
                    Text="Desactivar sensor"
                    Clicked="OnToggleSensorClicked"
                    BackgroundColor="#1565C0"
                    TextColor="White"
                    FontAttributes="Bold"
                    FontSize="16"
                    CornerRadius="10"
                    HeightRequest="55"
                    Margin="20,10"/>

        </VerticalStackLayout>

        <Button Text="🌙"
                Clicked="ToggleDarkMode"
                BackgroundColor="#7BA6FF"
                TextColor="#2C2C5A"
                FontAttributes="Bold"
                CornerRadius="25"
                WidthRequest="50"
                HeightRequest="50"
                HorizontalOptions="End"
                VerticalOptions="Start"
                Margin="20" />
    </Grid>
</local:BaseShake>